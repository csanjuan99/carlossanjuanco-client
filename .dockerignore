.git
.github
.gitignore
node_modules
dist
coverage

# Never send secrets into the build context. The runtime stage only copies
# dist/, but the build stage runs COPY . . and would otherwise receive them.
.env
.env.*
!.env.example

*.log
.DS_Store
.idea
.vscode

# Agent / tooling metadata and design docs: not part of the bundle.
.agents
.atl
.codex
.gga
openspec
docs
AGENTS.md
CLAUDE.md
DESIGN.md
PRODUCT.md
README.md
skills-lock.json

deploy.sh
compose.yaml
Dockerfile
.dockerignore

# nginx.conf is NOT excluded: the runtime stage copies it into the image.
